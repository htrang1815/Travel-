import React from "react";

const AccordionContent = ({ children }) => {
  return (
    <div>
      <div className="text-[14px] text-[#aaa]">{children}</div>
    </div>
  );
};

export default AccordionContent;
